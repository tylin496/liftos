name = "liftos-image-upload"
main = "src/index.ts"
compatibility_date = "2024-01-01"

# Secrets (set via `wrangler secret put <NAME>`, never committed):
#   GITHUB_TOKEN        fine-grained PAT, Contents: Read and write, scoped to tylin496/liftos only
#   SUPABASE_JWT_SECRET  Supabase project Settings > API > JWT Secret
#
# Vars (safe to commit, not secret):
[vars]
GITHUB_REPO = "tylin496/liftos"
OWNER_EMAIL = "tylin496@gmail.com"
